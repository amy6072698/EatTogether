<!DOCTYPE html>
<html lang="zh-Tw">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>@ViewData["Title"] | 餐廳管理系統</title>

    <!-- 引入第三方資源 -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css" integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://cdn.jsdelivr.net/npm/simple-datatables@7.1.2/dist/style.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">


    <!-- 自訂全站的 CSS 樣式 -->
    <link href="~/css/styles.css" rel="stylesheet" />

    <!-- 插入各頁面的 CSS -->
    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    @{
        var ctrl = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
        var action = ViewContext.RouteData.Values["action"]?.ToString() ?? "";

        // 判斷各折疊群組是否要展開
        bool inUser = ctrl == "Users";
        bool inMember = ctrl == "Members";
        bool inDish = ctrl is "Categories" or "Dishes" or "SetMeals";
        bool inCoupon = ctrl == "Coupons";
    }
    <!-- Navbar -->
    <nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark d-flex justify-content-between">
        <div>
            <a class="navbar-brand ps-3 fw-bold" asp-controller="Home" asp-action="Index">義起吃 Eat Together</a>
            <button class="btn btn-link btn-sm order-1 order-lg-0 ms-4 me-4 me-lg-0" id="sidebarToggle" href="#!"><i class="fas fa-bars"></i></button>
        </div>

        <ul class="navbar-nav d-flex align-items-center gap-2 ms-auto ms-md-0 me-3 me-lg-4">
            <li class="nav-item">
                <a class="nav-link d-flex gap-2" href="#" role="button">
                    <div class="navbar-user-avatar">
                        <img src="https://truth.bahamut.com.tw/s01/201312/1c0606db559be4848c1b53d01bda2efd.JPG" alt="user avatar" />
                    </div>
                    <div class="d-flex flex-column lh-sm">
                        <span class="fw-semibold text-white small">陳怡伶</span>
                        <span class="text-white-50" style="font-size:0.75rem;">店長</span>
                    </div>
                </a>
            </li>

            <li class="nav-item">
                <a class="btn btn-outline-light"
                   asp-controller="Auth" asp-action="Logout" role="button">
                    <i class="fa-solid fa-right-from-bracket me-1"></i>登出
                </a>
            </li>
        </ul>
    </nav>
    <div id="layoutSidenav">
        <!-- Sidenav Menu-->
        <div id="layoutSidenav_nav">
            <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
                <div class="sb-sidenav-menu">
                    <div class="nav">
                        <div class="sb-sidenav-menu-heading">人員管理</div>

                        <a class="nav-link @(inUser ? "" : "collapsed")" href="#" data-bs-toggle="collapse" data-bs-target="#collapseUser" aria-expanded="@(inUser ? "true" : "false")" aria-controls="collapseUser">
                            <div class="sb-nav-link-icon"><i class="fas fa-user-tie"></i></div>
                            員工管理
                            <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                        </a>
                        <div class="collapse @(inUser ? "show" : "")" id="collapseUser" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                            <nav class="sb-sidenav-menu-nested nav">
                                <a class="nav-link @(ctrl == "Users" && action == "Index" ? "active" : "")" asp-controller="Home" asp-action="Index">員工管理</a>
                                <a class="nav-link" asp-controller="Home" asp-action="Index">權限管理</a>
                            </nav>
                        </div>



                        <a class="nav-link @(inMember ? "" : "collapsed")" href="#" data-bs-toggle="collapse" data-bs-target="#collapseMember" aria-expanded="@(inMember ? "true" : "false")" aria-controls="collapseMember">
                            <div class="sb-nav-link-icon"><i class="fas fa-users"></i></div>
                            會員管理
                            <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                        </a>
                        <div class="collapse @(inMember ? "show" : "")" id="collapseMember" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                            <nav class="sb-sidenav-menu-nested nav">
                                <a class="nav-link @(ctrl == "Members" ? "active" : "")" asp-controller="Home" asp-action="Index">會員管理</a>
                            </nav>
                        </div>



                        <a class="nav-link" asp-controller="Home" asp-action="Index">
                            <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                            會員管理
                        </a>


                        <div class="sb-sidenav-menu-heading">菜單管理</div>

                        <a class="nav-link @(inDish ? "" : "collapsed")" href="#" data-bs-toggle="collapse" data-bs-target="#collapseDish" aria-expanded="@(inDish ? "true" : "false")" aria-controls="collapseDish">
                            <div class="sb-nav-link-icon"><i class="fas fa-utensils"></i></div>
                            菜單管理
                            <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                        </a>
                        <div class="collapse @(inDish ? "show" : "")" id="collapseDish" aria-labelledby="headingDish" data-bs-parent="#sidenavAccordion">
                            <nav class="sb-sidenav-menu-nested nav">
                                <a class="nav-link @(ctrl == "Categories" ? "active" : "")" asp-controller="Categories" asp-action="Index">類別</a>
                                <a class="nav-link @(ctrl == "Dishes" ? "active" : "")" asp-controller="Dishes" asp-action="Index">餐點</a>
                                <a class="nav-link @(ctrl == "SetMeals" ? "active" : "")" asp-controller="SetMeals" asp-action="Index">套餐</a>
                            </nav>
                        </div>


                        <div class="sb-sidenav-menu-heading">訂單管理</div>

                        <a class="nav-link @(ctrl == "Tables" ? "active" : "")" asp-controller="Tables" asp-action="Index">
                            <div class="sb-nav-link-icon"><i class="fas fa-border-all"></i></div>
                            桌位管理
                        </a>


                        <div class="sb-sidenav-menu-heading">預約管理</div>

                        <a class="nav-link @(ctrl == "Reservations" ? "active" : "")" asp-controller="Reservations" asp-action="Index">
                            <div class="sb-nav-link-icon"><i class="fas fa-calendar-check"></i></div>
                            訂位管理
                        </a>


                        <div class="sb-sidenav-menu-heading">行銷管理</div>

                        <a class="nav-link @(inCoupon ? "" : "collapsed")" href="#" data-bs-toggle="collapse" data-bs-target="#collapseCoupon" aria-expanded="@(inCoupon ? "true" : "false")" aria-controls="collapseCoupon">
                            <div class="sb-nav-link-icon"><i class="fas fa-ticket-alt"></i></div>
                            優惠券管理
                            <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                        </a>
                        <div class="collapse @(inCoupon ? "show" : "")" id="collapseCoupon" aria-labelledby="headingCoupon" data-bs-parent="#sidenavAccordion">
                            <nav class="sb-sidenav-menu-nested nav">
                                <a class="nav-link @(ctrl == "Coupons" && action == "Index" ? "active" : "")" asp-controller="Coupons" asp-action="Index">優惠券列表</a>
                                <a class="nav-link @(ctrl == "Coupons" && action == "MemberCoupons" ? "active" : "")" asp-controller="Coupons" asp-action="MemberCoupons">領券紀錄</a>
                            </nav>
                        </div>






                        <div class="sb-sidenav-footer-blank"></div>
                    </div>
                </div>
            </nav>
        </div>

        <div id="layoutSidenav_content">
            <!-- Main-->
            <main>
                <!-- 每個 View 的內容會渲染在這裡 -->
                @RenderBody()
            </main>

            <!-- Footer -->
            <footer class="py-4 bg-light mt-auto">
                <div class="container-fluid px-4">
                    <div class="d-flex align-items-center justify-content-between small">
                        <div class="text-muted">義起吃 Eat Together &copy;2026 Copyrights All Rights Reserved</div>
                        <div class="text-muted">
                            本專題僅為學習用途，不做任何商業使用。
                        </div>
                    </div>
                </div>
            </footer>
        </div>
    </div>



    <!-- 引入第三方資源 -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>

    <!-- 自訂全站的 JS -->
    <script src="~/js/scripts.js"></script>

    <!-- 插入各頁面的 JS -->
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>