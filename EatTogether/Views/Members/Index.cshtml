@model EatTogether.Models.ViewModels.MemberIndexViewModel

@{
    ViewData["Title"] = "會員管理";
    var statusFrontend = Model.Search.Status switch
    {
        "Normal" => "active",
        "Unconfirmed" => "unverified",
        "Blacklisted" => "blacklisted",
        "Deleted" => "deleted",
        _ => ""
    };
    var sortValue = Model.Search.SortBy == "CreatedAt_Asc" ? "CreatedAtAsc" : "CreatedAtDesc";
}

<div class="container-fluid px-4 members-index">

    <!-- 頁面標題列 -->
    <div class="d-flex align-items-start justify-content-between mt-4 mb-4">
        <h1 class="fw-bold">會員管理</h1>
        <div>
            <select id="sort-select" class="form-select form-select-sm" style="width:auto;">
                <option value="CreatedAtDesc" selected="@(sortValue == "CreatedAtDesc" ? "selected" : null)">註冊日期（最新）</option>
                <option value="CreatedAtAsc" selected="@(sortValue == "CreatedAtAsc" ? "selected" : null)">註冊日期（最舊）</option>
            </select>
        </div>
    </div>



    <!-- 搜尋列 -->
    <div class="card mb-3">
        <div class="card-body py-3">
            <div class="row g-2 align-items-center">
                 <div class="col-md col-sm-6">
                    <div class="search-field">
                        <label class="search-label" for="search-name">姓名</label>
                        <input type="text" class="form-control form-control-sm" id="search-name"
                               value="@Model.Search.Name" />
                    </div>
                </div>
                <div class="col-md col-sm-6">
                    <div class="search-field">
                        <label class="search-label" for="search-account">帳號</label>
                        <input type="text" class="form-control form-control-sm" id="search-account"
                               value="@Model.Search.Account" />
                    </div>
                </div>
                <div class="col-md col-sm-6">
                    <div class="search-field">
                        <label class="search-label" for="search-email">Email</label>
                        <input type="text" class="form-control form-control-sm" id="search-email"
                               value="@Model.Search.Email" />
                    </div>
                </div>
                <div class="col-md col-sm-6">
                    <div class="search-field">
                        <label class="search-label" for="search-phone">手機</label>
                        <input type="text" class="form-control form-control-sm" id="search-phone"
                               value="@Model.Search.Phone" />
                    </div>
                </div>
                <div class="col-md col-sm-6">
                    <div class="search-field">
                        <label class="search-label" for="search-status">狀態</label>
                        <select class="form-select form-select-sm" id="search-status">
                            <option value="" selected="@(statusFrontend == "" ? "selected" : null)">全部</option>
                            <option value="active" selected="@(statusFrontend == "active" ? "selected" : null)">正常（啟用中）</option>
                            <option value="unverified" selected="@(statusFrontend == "unverified" ? "selected" : null)">未驗證</option>
                            <option value="blacklisted" selected="@(statusFrontend == "blacklisted" ? "selected" : null)">黑名單</option>
                            <option value="deleted" selected="@(statusFrontend == "deleted" ? "selected" : null)">已刪除</option>
                        </select>
                    </div>
                </div>
                <div class="col-auto">
                    <button id="btn-search" class="btn btn-primary">
                        <i class="fa-solid fa-magnifying-glass me-1"></i>查詢
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- 會員資料表格 -->
    <div class="card">
        <div class="card-body">
            <table id="members-table" class="table table-bordered table-hover w-100">
                <thead>
                    <tr>
                        <th>姓名</th>
                        <th>帳號</th>
                        <th>Email</th>
                        <th>手機</th>
                        <th>生日</th>
                        <th>註冊時間</th>
                        <th>刪除時間</th>
                        <th>狀態</th>
                        <th class="text-center">操作</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var row in Model.Rows)
                    {
                        <tr data-id="@row.Id"
                            data-name="@row.Name"
                            data-account="@row.Account"
                            data-email="@row.Email"
                            data-phone="@row.Phone"
                            data-birthdate="@row.BirthDate?.ToString("yyyy-MM-dd")"
                            data-created-at="@row.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")"
                            data-deleted-at="@(row.DeletedAt?.ToString("yyyy-MM-dd HH:mm:ss") ?? "")"
                            data-status="@row.StatusColor"
                            data-blacklist-reason="@(row.BlacklistReason ?? "")">
                            <td>@row.Name</td>
                            <td>@row.Account</td>
                            <td>@row.Email</td>
                            <td>@(row.Phone ?? "—")</td>
                            <td>@(row.BirthDate?.ToString("yyyy-MM-dd") ?? "—")</td>
                            <td>@row.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")</td>
                            <td>@(row.DeletedAt?.ToString("yyyy-MM-dd HH:mm:ss") ?? "")</td>
                            <td>
                                <span class="status-badge @row.StatusColor">@row.StatusText</span>
                            </td>
                            <td>
                                <div class="action-btn-group">
                                    @if (row.ButtonType == "blacklist")
                                    {
                                        <button class="btn btn-sm btn-outline-danger btn-blacklist-member"
                                                data-id="@row.Id" data-name="@row.Name" title="加入黑名單">
                                            <i class="fa-solid fa-ban"></i>
                                        </button>
                                    }
                                    else if (row.ButtonType == "unblacklist")
                                    {
                                        <button class="btn btn-sm btn-outline-success btn-unblacklist-member"
                                                data-id="@row.Id" data-name="@row.Name" title="解除黑名單">
                                            <i class="fa-solid fa-circle-check"></i>
                                        </button>
                                    }
                                    else if (row.ButtonType == "disabled")
                                    {
                                        <button class="btn btn-sm btn-outline-danger btn-blacklist-member"
                                                data-id="@row.Id" data-name="@row.Name" title="加入黑名單" disabled>
                                            <i class="fa-solid fa-ban"></i>
                                        </button>
                                    }
                                    <button class="btn btn-sm btn-outline-primary btn-detail-member"
                                            data-id="@row.Id" title="查看詳情">
                                        <i class="fa-solid fa-eye"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>



<!-- ============================================================
     會員詳情 Modal（唯讀）
     ============================================================ -->
<div class="modal fade" id="modal-member-detail" tabindex="-1" aria-labelledby="modal-member-detail-label" aria-hidden="true">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal-member-detail-label">
                    <i class="fa-solid fa-user me-2 text-primary"></i>會員詳情
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="關閉"></button>
            </div>
            <div class="modal-body members-index">
                <div class="detail-row">
                    <span class="detail-label">姓名</span>
                    <span class="detail-value" id="detail-name"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">帳號</span>
                    <span class="detail-value" id="detail-account"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Email</span>
                    <span class="detail-value" id="detail-email"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">手機</span>
                    <span class="detail-value" id="detail-phone"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">生日</span>
                    <span class="detail-value" id="detail-birthdate"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">註冊時間</span>
                    <span class="detail-value" id="detail-created-at"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">刪除時間</span>
                    <span class="detail-value" id="detail-deleted-at"></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">狀態</span>
                    <span class="detail-value" id="detail-status"></span>
                </div>
                <div class="detail-row" id="detail-reason-row">
                    <span class="detail-label">黑名單原因</span>
                    <span class="detail-value" id="detail-blacklist-reason"></span>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">關閉</button>
            </div>
        </div>
    </div>
</div>


@section Styles {
    <link rel="stylesheet" href="~/css/views/members-index.css" />
}

@section Scripts {
    <script src="~/js/views/members-index.js"></script>
}
